/*
 * Tiang theme: SpacePanel — vertical (top → bottom) light blue gradient.
 */

.mx_SpacePanel {
    background: linear-gradient(180deg, #e8f4fc 0%, #d4e8f8 100%);
}

.mx_SpacePanel.newUi {
    background: linear-gradient(180deg, #e8f4fc 0%, #d4e8f8 100%);
    border-right: none;
}
